#' reading control.def
#'
# so_prj <- 'd:/so21302/rhein29a.lit'
# so_dir <- 'd:/so21302'
# cdef.open <- get_file_path(case.name = 'BFG_PZ27_TEST',
#                        so_prj, type = 'control.def'
#                        )
# cdat.open <- get_file_path(case.name = 'BFG_PZ27_TEST',
#                            so_prj, type = 'control.dat'
# )
# cdef.close <- get_file_path(case.name = 'BFG_BZ18_TEST',
#                            so_prj, type = 'control.def'
# )
# cdat.close <- get_file_path(case.name = 'BFG_BZ18_TEST',
#                            so_prj, type = 'control.dat'
# )
# sconf_open <- read.config(sfile)
# def.open <- fread(file = cdef.open, header = F, sep = "\n")
